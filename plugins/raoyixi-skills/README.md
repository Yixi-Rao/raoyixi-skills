# Raoyixi Skills Plugin

This Codex plugin packages selected personal skills for reuse across machines.

The plugin exposes only `skills/`. It does not install hooks, MCP servers, app integrations, credentials, or machine-local state.
